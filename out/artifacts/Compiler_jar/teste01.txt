start 
 int a;
 int b, result,y;
 float x,total;
 a = 2;
 x = 2.1;
 y = 1;
 scan (b);
 scan (y);
 result = (a*y + 1) % a;
 print ({Resultado: });
 print (result);
 print ({Total: });

 total = y / x;
 print ({Total: });
 print (total);
exit